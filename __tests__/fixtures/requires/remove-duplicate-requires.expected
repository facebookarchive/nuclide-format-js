import type N from 'N';
import typeof O from 'O';
import type {A, B} from 'Z';

require('P');

const M = require('M');
const [j, k] = require('Y');
const {a, b} = require('Z');

const x: A<B, N, O> = a(b, M, j, k);
